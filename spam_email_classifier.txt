# -*- coding: utf-8 -*-
"""spam email classifier.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1DUF43nM5_DqvxpTvgPo_Lb-k7TlI1i6U

### Kaggle Dataset Link
https://www.kaggle.com/rockinjas123/spam-ham-emails
"""

import pandas as pd
import numpy as np
import seaborn as sb
from collections import defaultdict
from sklearn.model_selection import train_test_split
from sklearn.metrics import accuracy_score
from nltk.stem import PorterStemmer
from nltk.tokenize import word_tokenize
import nltk
nltk.download('punkt')

from google.colab import drive
drive.mount('/content/drive')

df = pd.read_csv("/content/emails.csv")

df

df['spam'].value_counts()

sb.countplot(x= df['spam'])

"""## Data Wrangling"""

df.duplicated()

df.duplicated().sum()

df['text'].duplicated().sum()

df['spam'].duplicated().sum()

"""#### Handling duplicates values"""

df.drop_duplicates(inplace=True)

df.duplicated('text').sum()

df.duplicated('spam').sum()

df.shape

df

df.isnull().sum()

"""#### Spliting the dataset"""

x = df.text.values
y = df.spam.values

"""#### Text to countvectorizer"""

from sklearn.feature_extraction.text import TfidfVectorizer

tfid = TfidfVectorizer()
x_tfid = tfid.fit_transform(x)

x_tfid.toarray()

from sklearn.naive_bayes import MultinomialNB
from sklearn.feature_extraction.text import TfidfVectorizer
from sklearn.pipeline import make_pipeline

pipeline = make_pipeline(TfidfVectorizer(), MultinomialNB())

param_grid = {
    'tfidfvectorizer__max_features': [500, 1000, 5000],
    'tfidfvectorizer__ngram_range': [(1, 1), (1, 2)],
    'multinomialnb__alpha': [0.01, 0.1, 1]
}

"""#### Data spliting to train test"""

# @title Default title text

def stem_text(data):

    stemmer = PorterStemmer()
    stemmed_texts = []
    for text in data:
        words = word_tokenize(text)
        stemmed_words = [stemmer.stem(word) for word in words]
        stemmed_text = " ".join(stemmed_words)
        stemmed_texts.append(stemmed_text)
        return stemmed_texts

stemmed_data = stem_text(df['text'])
for text in stemmed_data:
    print(text)

from sklearn.model_selection import train_test_split
from sklearn.model_selection import GridSearchCV

xtrain, xtest, ytrain, ytest = train_test_split(x_tfid,y, test_size = 0.30)

xtrain_dense = xtrain.toarray()

xtrain_dense[np.isnan(xtrain_dense)] = np.nanmean(xtrain_dense)  # Replace NaNs with the mean of non-NaN elements

"""## Model Fit

#### Multinomial Naive Bias
"""

model_accuracies = {}

from sklearn.naive_bayes import MultinomialNB
model_nb = MultinomialNB()
model_nb.fit(xtrain, ytrain)

print("Multinomial Naive Bayes Test Score:", model_nb.score(xtest, ytest))

nb_accuracy = model_nb.score(xtest, ytest)
model_accuracies['Multinomial Naive Bayes'] = nb_accuracy

"""#### Gaussian naive bias

#### Bernoulli Naive Bias
"""

from sklearn.naive_bayes import BernoulliNB
bnb = BernoulliNB()
bnb.fit(xtrain,ytrain)
print("Bernoulli Naive Bayes Test Score:", bnb.score(xtest, ytest))

bnb_accuracy = bnb.score(xtest, ytest)
model_accuracies['Bernoulli Naive Bayes'] = bnb_accuracy

x_train, x_test, y_train, y_test = train_test_split(x, y, test_size=0.30, random_state=42)

grid_search = GridSearchCV(pipeline, param_grid, cv=5, verbose=2)
#grid_search = GridSearchCV(pipeline, param_grid, cv=5, verbose=3, error_score='raise')
grid_search.fit(x_train, y_train)
print("Best parameters:", grid_search.best_params_)

test_accuracy = grid_search.score(x_test, y_test)
print("Test accuracy: {:.2f}%".format(test_accuracy * 100))

from sklearn.ensemble import RandomForestClassifier
from sklearn.metrics import accuracy_score

# Assuming X_train, X_test, y_train, y_test are already defined
rf_classifier = RandomForestClassifier()  # You can tune these parameters
rf_classifier.fit(xtrain, ytrain)
print("Random Forest Test Score:", rf_classifier.score(xtest, ytest))

rf_accuracy = rf_classifier.score(xtest, ytest)
model_accuracies['Random Forest'] = rf_accuracy

from sklearn.svm import SVC

svm_model = SVC(kernel='linear')  # You can try different kernels like 'rbf'
svm_model.fit(xtrain, ytrain)
print("SVM Test Score:", svm_model.score(xtest, ytest))

svm_accuracy = svm_model.score(xtest, ytest)
model_accuracies['Support Vector Machine'] = svm_accuracy

comparison_df = pd.DataFrame(
    list(model_accuracies.items()),
    columns=['Model', 'Accuracy']
)

comparison_df['Accuracy'] = (comparison_df['Accuracy'] * 100).round(2).astype(str) + '%'
comparison_df.sort_values(by='Accuracy', ascending=False, inplace=True)

print(comparison_df)